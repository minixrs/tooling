From f1db256ff447b4e5fb41dffed58972a9fd133d10 Mon Sep 17 00:00:00 2001
From: Kevin Barnard <kevin.barnard@gmail.com>
Date: Sun, 26 Jul 2026 18:21:24 -0500
Subject: [PATCH] slice 5.6a: port musl to minix.rs IPC (aarch64)

Replace musl's Linux syscall ABI with minix.rs message passing, so ordinary C
compiled against this libc runs on a MINIX-style microkernel where every OS
service is a user-space server.

The delta is deliberately tiny -- every file that stays byte-identical to
upstream rebases for free onto the next release:

  arch/aarch64/syscall_arch.h      __syscall0..6 -> __minixrs_syscall; VDSO gone
  src/thread/aarch64/syscall_cp.s  __syscall_cp_asm forwards to the port
  src/minixrs/ipc.c                the svc #0 IPC trap
  src/minixrs/_syscall.c           Linux syscall number -> server IPC
  src/minixrs/minixrs_internal.h   shared declarations
  crt/crt1.c                       the .note.minixrs.ident ELF brand
  MINIXRS.md                       branch + delta contracts, Linux inventory

musl's ~297 __syscall call sites are untouched; that seam is what lets the
other ~1900 source files stay identical. src/minixrs/ needs no Makefile edit --
musl globs src/*/. syscall_ret.c is untouched because minix.rs errno magnitudes
(1..=40 and >=200) all sit inside its > -4096UL window.

Mapped: writev/write -> VFS_WRITE, exit/exit_group -> PM_EXIT, set_tid_address
-> 1, ioctl -> -ENOTTY, everything else -ENOSYS. The ioctl answer is
load-bearing: it makes __stdout_write set f->lbf = -1, so stdout is fully
buffered and printf is flushed by exit()'s __stdio_exit.

syscall_cp.s is in the delta because redirecting syscall_arch.h alone is not
enough. Cancellable syscalls (read/write/open/close) reach __syscall_cp, and
which implementation that resolves to is decided by LINKAGE: __syscall_cp.c
supplies a weak __syscall_cp_c routing through the port, pthread_cancel.c a
strong one calling __syscall_cp_asm, which upstream ends in a bare `svc 0`. A
program touching pthread_cancel would silently route every cancellable syscall
onto a raw Linux trap -- which on minix.rs is read as an endpoint, a primitive
and a message pointer. It now forwards to __minixrs_syscall regardless.

Three asm files still hold a raw Linux svc (__restore_rt, __clone,
__unmapself). None is reachable without threads or signal delivery, and porting
them means inventing semantics minix.rs does not have yet, so MINIXRS.md
records them with the condition that makes each reachable.

Verified: a C hello world exec'd from the minix.rs boot archive reaches serial
through printf -> VFS -> TTY; write(3,...) returns -1/EBADF; the two-iovec
writev path is exercised and mutation-tested. A single-threaded binary contains
exactly one svc, in __minixrs_ipc.

Based on upstream v1.2.6.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
---
 MINIXRS.md                      | 243 ++++++++++++++++++++++++++++++++
 arch/aarch64/syscall_arch.h     |  77 +++++-----
 crt/crt1.c                      |  34 +++++
 src/minixrs/_syscall.c          | 199 ++++++++++++++++++++++++++
 src/minixrs/ipc.c               |  40 ++++++
 src/minixrs/minixrs_internal.h  |  43 ++++++
 src/thread/aarch64/syscall_cp.s |  52 +++++--
 7 files changed, 633 insertions(+), 55 deletions(-)
 create mode 100644 MINIXRS.md
 create mode 100644 src/minixrs/_syscall.c
 create mode 100644 src/minixrs/ipc.c
 create mode 100644 src/minixrs/minixrs_internal.h

diff --git a/MINIXRS.md b/MINIXRS.md
new file mode 100644
index 00000000..f5162cd8
--- /dev/null
+++ b/MINIXRS.md
@@ -0,0 +1,243 @@
+# musl-minixrs — the minix.rs libc fork
+
+This is [musl libc](https://musl.libc.org/) with its Linux syscall ABI replaced
+by [minix.rs](https://github.com/minixrs/minixrs) IPC. It exists so that
+ordinary C programs — compiled with an unmodified clang, linked against an
+almost-unmodified musl — run on a MINIX-style microkernel where every OS
+service is a user-space server reached by message passing.
+
+musl is MIT licensed; see `COPYRIGHT`. This fork adds nothing under a different
+licence.
+
+## Branch layout
+
+| Branch | What it is |
+|---|---|
+| `main` | A **pristine mirror** of upstream `master`. Never commit to it. |
+| `minixrs` | The port. Based on tag **`v1.2.6`** (2026-03-20). The default branch. |
+
+`main` exists so the fork is diffable against upstream without a second remote.
+Refresh it with:
+
+```sh
+git fetch upstream
+git checkout main
+git merge --ff-only upstream/master
+```
+
+The port branch is pinned to a **tag**, never to `main`'s head — a libc that
+moves under you is not a base you can debug a kernel against. Moving to a new
+release is a deliberate act:
+
+```sh
+git rebase --onto v1.2.7 v1.2.6 minixrs
+git push --force-with-lease origin minixrs
+```
+
+Because the port branch is force-pushed, **the minix.rs submodule pin must be
+bumped in the same PR as any rebase** (`external/musl` in the minixrs repo). A
+rebase orphans the previously pinned SHA, and a submodule pointing at an
+orphaned commit fails to clone rather than failing loudly at build time.
+
+## Delta contract
+
+The port is deliberately tiny, and staying tiny is a design goal rather than an
+accident — every file that stays byte-identical to upstream is a file that
+rebases for free onto the next release. **The entire minix.rs footprint is:**
+
+| Path | What it does |
+|---|---|
+| `arch/aarch64/syscall_arch.h` | `__syscall0..6` call `__minixrs_syscall`; VDSO macros deleted |
+| `src/thread/aarch64/syscall_cp.s` | `__syscall_cp_asm` forwards to the port instead of `svc` |
+| `src/minixrs/ipc.c` | The `svc #0` IPC trap |
+| `src/minixrs/_syscall.c` | Linux syscall number → server IPC |
+| `src/minixrs/minixrs_internal.h` | Shared declarations for the two above |
+| `crt/crt1.c` | The 28-byte `.note.minixrs.ident` ELF brand |
+| `MINIXRS.md` | This file |
+
+Nothing else. `git diff v1.2.6 --stat` should show exactly three modified files
+outside `src/minixrs/`.
+
+### Why `syscall_cp.s` is in the delta
+
+It is not enough to redirect `syscall_arch.h`. Cancellable syscalls — `read`,
+`write`, `open`, `close`, … — go through `__syscall_cp`, and **which
+implementation that resolves to is decided by linkage**:
+
+- `src/thread/__syscall_cp.c` supplies a **weak** `__syscall_cp_c` that calls
+  `__syscall`, and therefore the port.
+- `src/thread/pthread_cancel.c` supplies a **strong** one that calls
+  `__syscall_cp_asm`, which upstream ends with a bare `svc 0`.
+
+A program that never touches `pthread_cancel` gets the safe path by accident. A
+program that does silently routes every cancellable syscall onto a raw Linux
+trap — which on minix.rs means the kernel reads x0/x1/x2 as an endpoint, a
+primitive, and a message pointer. So `__syscall_cp_asm` forwards to
+`__minixrs_syscall` instead, and the behaviour no longer depends on what else
+got linked.
+
+Cancellation itself is a no-op (no threads yet); the `__cp_begin`/`__cp_end`/
+`__cp_cancel` labels are preserved so the file's shape survives until a slice
+adds threads.
+
+### Linux traps that deliberately remain
+
+Three assembly files still contain a raw Linux `svc`, and none is reachable
+today. Porting them would mean inventing a threading and signal-delivery model
+minix.rs does not have yet, so they are recorded rather than rewritten:
+
+| File | Symbol | Becomes reachable when |
+|---|---|---|
+| `src/signal/aarch64/restore.s` | `__restore_rt` | signal handlers are delivered to user space |
+| `src/thread/aarch64/clone.s` | `__clone` | threads exist |
+| `src/thread/aarch64/__unmapself.s` | `__unmapself` | threads exist (teardown) |
+
+Audit any linked binary with:
+
+```sh
+llvm-objdump -d <binary> | awk '/^[0-9a-f]+ </{fn=$2} /\tsvc\t/{print fn, $0}'
+```
+
+A single-threaded C program must show exactly **one** hit, in `__minixrs_ipc`.
+
+**No Makefile change is needed** to build `src/minixrs/`: musl globs its source
+directories (`SRC_DIRS = $(addprefix $(srcdir)/,src/* …)` in the `Makefile`), so
+a new `src/<name>/` directory is picked up automatically. Verified against the
+v1.2.6 tree.
+
+`src/internal/syscall_ret.c` is **untouched on purpose**. It treats a return
+value in `[-4095, -1]` as an errno, and minix.rs errno magnitudes are `1..=40`
+and `>= 200` (phase-5 decision D7), so all of them land inside that window. The
+POSIX block additionally uses the same values Linux and musl do, which is why
+musl's stock `bits/errno.h` needs no edit either.
+
+## The seam
+
+musl's ~297 `__syscall` call sites are all unmodified. They still emit Linux
+syscall numbers. `arch/aarch64/syscall_arch.h` is the one place that knows
+better: instead of executing `svc` with the number in `x8`, every `__syscallN`
+calls `__minixrs_syscall`, which switches on the Linux number and issues the
+corresponding server round-trip.
+
+Keeping that shape — rather than rewriting call sites — is what lets the other
+~1900 source files stay identical to upstream.
+
+The IPC trap ABI is fixed by the kernel's `trap.S` and reproduced exactly in
+`src/minixrs/ipc.c`. It is the C twin of the Rust `minixrs-ipc` crate:
+
+```
+x0 = endpoint (also receives the result)
+x1 = IPC primitive (SEND / RECEIVE / SENDREC / NOTIFY / SENDNB)
+x2 = pointer to the 104-byte message
+svc #0
+```
+
+### Implemented syscalls
+
+| Linux syscall | minix.rs mapping |
+|---|---|
+| `writev` | one `VFS_WRITE` per iovec, counts summed; partial progress reported on a mid-loop error |
+| `write` | one `VFS_WRITE` |
+| `exit_group`, `exit` | `PM_EXIT` (does not return) |
+| `set_tid_address` | constant tid `1` |
+| `ioctl` | `-ENOTTY` |
+| everything else | `-ENOSYS` |
+
+`ioctl` returning `-ENOTTY` is load-bearing rather than merely harmless:
+`__stdout_write` sets `f->lbf = -1` when `TIOCGWINSZ` fails, which makes stdout
+**fully buffered**. `printf` output is then flushed by `exit()`'s
+`__stdio_exit`, so the flush path is exercised by any program that prints.
+
+`writev` is the one that carries stdio — `__stdio_write` always calls `writev`,
+never `write`, and loops until the returned count equals what it asked for. The
+count returned must therefore be exact; over-reporting makes musl believe a
+flush completed that did not.
+
+## Linux dependency inventory (re-derived against v1.2.6)
+
+musl references **341 distinct `SYS_*` numbers** across 297 call sites. The port
+implements six. That is viable only because of what the startup path does *not*
+do — each of the following was re-read in the v1.2.6 source, not assumed:
+
+| Fact | Consequence |
+|---|---|
+| `__init_libc` returns early when `AT_UID==AT_EUID && AT_GID==AT_EGID && !AT_SECURE` — all read `0` when absent from the auxv | the secure-mode branch, the **only** `ppoll`/`open` in startup, never runs |
+| `__init_ssp(NULL)` (no `AT_RANDOM`) derives the canary as `&__stack_chk_guard * 1103515245` | no syscall |
+| `__init_tp` issues exactly one syscall, `set_tid_address` | `__set_thread_area` is `msr tpidr_el0` inline asm on aarch64 (v1.2.6 replaced the `.s` file) |
+| `__init_tls` uses the static `builtin_tls` when the program has no `PT_TLS` | no `mmap` |
+| `__init_tls` walks `AT_PHDR` / `AT_PHNUM` / `AT_PHENT` | the kernel must supply them — minix.rs does, since slice 5.5 |
+| `__set_thread_area` walks `libc.auxv` to mask SME hwcap bits | the auxv **must** be `AT_NULL`-terminated |
+
+Tiers of what remains, roughly by distance:
+
+- **Next up (minix.rs slice 5.8+):** `openat`, `read`, `close`, `lseek` — real
+  file I/O, once there is a filesystem server. These are already referenced by
+  code that links today; they simply answer `-ENOSYS`.
+- **Needs kernel work:** `mmap`/`munmap`/`brk` (minix.rs has `VM_MMAP`/`VM_BRK`
+  but no libc wiring), `futex` (no threads yet), signal delivery
+  (`rt_sigaction` and friends — minix.rs signals default-terminate).
+- **Wholly Linux, unlikely to ever be ported:** everything under `src/linux/`
+  (`epoll`, `inotify`, `sendfile`, `prctl`, cgroup and namespace calls), plus
+  `src/aio`, `src/mq`, and the netlink paths in `src/network`. These compile
+  into `libc.a` and answer `-ENOSYS` if called; nothing links them in by
+  accident.
+
+## The ELF brand
+
+Every minix.rs user-space binary carries a 28-byte `.note.minixrs.ident` note
+(the normative spec is `docs/abi-note.md` in the minixrs tooling repo). The
+kernel **refuses to load an unbranded ELF**.
+
+The note is emitted from `crt/crt1.c` as a `.pushsection` global asm, so every C
+binary gets it regardless of how it was linked, and no program has to opt in.
+`crt1.o` is linked explicitly into every executable rather than pulled from an
+archive, so — unlike a `libc.a` member — it can never be dropped by
+archive-member selection.
+
+Its bytes are identical to what the Rust `minixrs_abi_note::brand!()` macro
+emits, so a C binary and a Rust one are indistinguishable to the verifier:
+
+```
+08 00 00 00              namesz = 8
+08 00 00 00              descsz = 8
+01 00 00 00              type   = NT_MINIXRS_IDENT
+6d 69 6e 69 78 72 73 00  "minixrs\0"
+01 00 00 00              abi_version = 1
+00 00 00 00              flags = 0
+```
+
+A linker script consuming this libc must `KEEP()` the section and give it a
+dedicated `PT_NOTE` at the start of the read-only `PT_LOAD`. Regressing the
+brand fails the **minix.rs kernel build**, not the boot: `kernel/build.rs` runs
+the same `scan_brand` check when it packs the boot archive.
+
+## Building
+
+The canonical build is `tools/build-musl.sh` in the minixrs repo, which drives
+configure/make into a cached sysroot at `target/musl-sysroot`. To build by hand:
+
+```sh
+mkdir build && cd build
+SR="$(rustc --print sysroot)"; AR=$(echo "$SR"/lib/rustlib/*/bin/llvm-ar)
+../configure --target=aarch64-linux-musl --disable-shared \
+  --prefix=/path/to/sysroot \
+  CC="clang --target=aarch64-unknown-linux-musl" \
+  AR="$AR" RANLIB="$AR s" \
+  CFLAGS="-I/path/to/minixrs/target/gen-c-headers/include"
+make && make install
+```
+
+Notes:
+
+- Build **out of tree**. The minixrs repo vendors this as a submodule and its
+  CI asserts a clean `git status`, so nothing may be written into the work tree.
+- `RANLIB="$AR s"` rather than `llvm-ranlib`: the pinned Rust toolchain ships
+  `llvm-ar` but **not** `llvm-ranlib`. `llvm-ar s` writes the archive index,
+  which is all `ranlib` is for here.
+- The `CFLAGS` `-I` is how `src/minixrs/*.c` finds `<minixrs/ipc.h>` under
+  musl's `-nostdinc`. Those headers are generated by `cargo gen-c-headers` in
+  the minixrs repo from the live Rust constants — they are a build artifact and
+  are never committed to either repo.
+- configure's linker probes all fail under Apple clang (its default linker is
+  ld64). That is harmless: `--disable-shared` means only `libc.a` and the crt
+  objects are produced, and consumers link with `rust-lld`.
diff --git a/arch/aarch64/syscall_arch.h b/arch/aarch64/syscall_arch.h
index 504983aa..7d482d07 100644
--- a/arch/aarch64/syscall_arch.h
+++ b/arch/aarch64/syscall_arch.h
@@ -1,78 +1,67 @@
 #define __SYSCALL_LL_E(x) (x)
 #define __SYSCALL_LL_O(x) (x)
 
-#define __asm_syscall(...) do { \
-	__asm__ __volatile__ ( "svc 0" \
-	: "=r"(x0) : __VA_ARGS__ : "memory", "cc"); \
-	return x0; \
-	} while (0)
+/*
+ * minix.rs is not Linux: there is no kernel entry point that decodes a Linux
+ * syscall number. The kernel's only `svc #0` ABI is the MINIX IPC trap
+ * (x0 = endpoint, x1 = primitive, x2 = &message), and every OS service lives
+ * in a user-space server reached by message passing.
+ *
+ * So nothing in this header traps. Each __syscallN funnels into
+ * __minixrs_syscall (src/minixrs/_syscall.c), which translates the handful of
+ * Linux numbers musl actually emits into server round-trips. The trap itself
+ * is __minixrs_ipc (src/minixrs/ipc.c).
+ *
+ * Keeping the __syscallN shape rather than rewriting musl's call sites is the
+ * whole point: it is the one seam that lets the other ~1900 source files stay
+ * byte-identical to upstream.
+ *
+ * src/internal/syscall_ret.c is deliberately untouched. It treats a return in
+ * [-4095, -1] as an errno, and minix.rs errno magnitudes are 1..=40 and >=200
+ * (phase-5 decision D7), so every one of them lands inside that window.
+ */
+hidden long __minixrs_syscall(long n, long a, long b, long c, long d, long e,
+                              long f);
 
 static inline long __syscall0(long n)
 {
-	register long x8 __asm__("x8") = n;
-	register long x0 __asm__("x0");
-	__asm_syscall("r"(x8));
+	return __minixrs_syscall(n, 0, 0, 0, 0, 0, 0);
 }
 
 static inline long __syscall1(long n, long a)
 {
-	register long x8 __asm__("x8") = n;
-	register long x0 __asm__("x0") = a;
-	__asm_syscall("r"(x8), "0"(x0));
+	return __minixrs_syscall(n, a, 0, 0, 0, 0, 0);
 }
 
 static inline long __syscall2(long n, long a, long b)
 {
-	register long x8 __asm__("x8") = n;
-	register long x0 __asm__("x0") = a;
-	register long x1 __asm__("x1") = b;
-	__asm_syscall("r"(x8), "0"(x0), "r"(x1));
+	return __minixrs_syscall(n, a, b, 0, 0, 0, 0);
 }
 
 static inline long __syscall3(long n, long a, long b, long c)
 {
-	register long x8 __asm__("x8") = n;
-	register long x0 __asm__("x0") = a;
-	register long x1 __asm__("x1") = b;
-	register long x2 __asm__("x2") = c;
-	__asm_syscall("r"(x8), "0"(x0), "r"(x1), "r"(x2));
+	return __minixrs_syscall(n, a, b, c, 0, 0, 0);
 }
 
 static inline long __syscall4(long n, long a, long b, long c, long d)
 {
-	register long x8 __asm__("x8") = n;
-	register long x0 __asm__("x0") = a;
-	register long x1 __asm__("x1") = b;
-	register long x2 __asm__("x2") = c;
-	register long x3 __asm__("x3") = d;
-	__asm_syscall("r"(x8), "0"(x0), "r"(x1), "r"(x2), "r"(x3));
+	return __minixrs_syscall(n, a, b, c, d, 0, 0);
 }
 
 static inline long __syscall5(long n, long a, long b, long c, long d, long e)
 {
-	register long x8 __asm__("x8") = n;
-	register long x0 __asm__("x0") = a;
-	register long x1 __asm__("x1") = b;
-	register long x2 __asm__("x2") = c;
-	register long x3 __asm__("x3") = d;
-	register long x4 __asm__("x4") = e;
-	__asm_syscall("r"(x8), "0"(x0), "r"(x1), "r"(x2), "r"(x3), "r"(x4));
+	return __minixrs_syscall(n, a, b, c, d, e, 0);
 }
 
 static inline long __syscall6(long n, long a, long b, long c, long d, long e, long f)
 {
-	register long x8 __asm__("x8") = n;
-	register long x0 __asm__("x0") = a;
-	register long x1 __asm__("x1") = b;
-	register long x2 __asm__("x2") = c;
-	register long x3 __asm__("x3") = d;
-	register long x4 __asm__("x4") = e;
-	register long x5 __asm__("x5") = f;
-	__asm_syscall("r"(x8), "0"(x0), "r"(x1), "r"(x2), "r"(x3), "r"(x4), "r"(x5));
+	return __minixrs_syscall(n, a, b, c, d, e, f);
 }
 
-#define VDSO_USEFUL
-#define VDSO_CGT_SYM "__kernel_clock_gettime"
-#define VDSO_CGT_VER "LINUX_2.6.39"
+/*
+ * No VDSO. minix.rs maps none, so VDSO_USEFUL / VDSO_CGT_SYM / VDSO_CGT_VER are
+ * deliberately absent: defining them would make clock_gettime search for a
+ * symbol in an object that does not exist.
+ */
 
 #define IPC_64 0
diff --git a/crt/crt1.c b/crt/crt1.c
index 10601215..f3a289a4 100644
--- a/crt/crt1.c
+++ b/crt/crt1.c
@@ -5,6 +5,40 @@
 
 #include "crt_arch.h"
 
+/*
+ * The minixrs ELF identity note (tooling/docs/abi-note.md, normative).
+ *
+ * The kernel REFUSES to load an unbranded ELF, so every C binary needs this
+ * 28-byte note. It lives in crt1.c because crt1.o is linked explicitly into
+ * every executable rather than pulled from an archive -- so it cannot be
+ * dropped by archive-member selection the way a libc.a member could, and no
+ * C program has to remember to opt in.
+ *
+ * Byte-for-byte the same record the Rust `minixrs_abi_note::brand!()` macro
+ * emits, so a C binary and a Rust one are indistinguishable to the verifier:
+ *
+ *   namesz = 8, descsz = 8, type = NT_MINIXRS_IDENT (1),
+ *   name = "minixrs\0", desc = [abi_version = 1, flags = 0]
+ *
+ * Both name and descriptor are already 4-byte multiples, so no padding is
+ * emitted. The linker script must KEEP() this section and give it a dedicated
+ * PT_NOTE at the start of the read-only PT_LOAD.
+ *
+ * Regressing this fails the minix.rs KERNEL BUILD, not the boot: kernel/build.rs
+ * runs the same scan_brand check when it packs the boot archive.
+ */
+__asm__(
+	".pushsection .note.minixrs.ident, \"a\", %note\n"
+	".p2align 2\n"
+	".long 8\n"
+	".long 8\n"
+	".long 1\n"
+	".asciz \"minixrs\"\n"
+	".long 1\n"
+	".long 0\n"
+	".popsection\n"
+);
+
 int main();
 weak void _init();
 weak void _fini();
diff --git a/src/minixrs/_syscall.c b/src/minixrs/_syscall.c
new file mode 100644
index 00000000..3b24ef26
--- /dev/null
+++ b/src/minixrs/_syscall.c
@@ -0,0 +1,199 @@
+/*
+ * Linux syscall numbers -> minix.rs server IPC.
+ *
+ * arch/aarch64/syscall_arch.h routes every __syscallN here. musl's call sites
+ * are unmodified, so what arrives is a Linux syscall number; what leaves is a
+ * MINIX-style message round-trip to a user-space server.
+ *
+ * The mapped set is deliberately tiny, because most of what a Linux libc needs
+ * a syscall for, minix.rs either does not have yet or does not need at startup.
+ * The facts below were re-verified against the v1.2.6 source rather than
+ * assumed, since the whole wrapper set is short only if they hold:
+ *
+ *   - __init_libc returns early: with AT_UID/AT_EUID/AT_GID/AT_EGID/AT_SECURE
+ *     all absent from our auxv they read 0, so the equality test passes and
+ *     the secure-mode branch (the only ppoll/open in startup) never runs.
+ *   - __init_ssp(NULL) -- no AT_RANDOM -- derives the canary arithmetically.
+ *   - __init_tp issues exactly one syscall, set_tid_address; on aarch64
+ *     __set_thread_area is `msr tpidr_el0` inline asm (v1.2.6 replaced the .s).
+ *   - __init_tls uses the static builtin_tls when the program has no PT_TLS,
+ *     so there is no mmap. It walks AT_PHDR/AT_PHNUM/AT_PHENT, which the
+ *     kernel supplies (slice 5.5).
+ *   - __set_thread_area walks libc.auxv to mask hwcap bits, so the auxv must be
+ *     AT_NULL-terminated -- it is.
+ *
+ * ioctl answering -ENOTTY is not merely harmless, it is load-bearing:
+ * __stdout_write sets f->lbf = -1 when TIOCGWINSZ fails, which makes stdout
+ * FULLY buffered. printf output is then flushed by exit()'s __stdio_exit, so
+ * the flush path is part of what a passing boot proves.
+ */
+
+#include <errno.h>
+#include <limits.h>
+#include <stddef.h>
+#include <stdint.h>
+#include <sys/syscall.h>
+#include <sys/uio.h>
+
+#include <minixrs/callnr.h>
+#include <minixrs/com.h>
+#include <minixrs/ipc.h>
+
+#include "minixrs_internal.h"
+
+/* Native-endian payload writes, matching the Rust servers' to_ne_bytes(). */
+static void put_i32(message *m, unsigned off, int32_t v)
+{
+	__builtin_memcpy(m->payload + off, &v, sizeof v);
+}
+
+static void put_u64(message *m, unsigned off, uint64_t v)
+{
+	__builtin_memcpy(m->payload + off, &v, sizeof v);
+}
+
+/*
+ * write(2) for one contiguous buffer, as a VFS_WRITE round-trip.
+ *
+ * The buffer travels as a RAW ADDRESS in this process's own address space, not
+ * as a grant: a C program is an ordinary user process with no grant table and
+ * no privilege to issue one. VFS turns the address into a magic grant naming
+ * this process as the owner -- and it takes that owner from the kernel-stamped
+ * m_source, never from the payload, which is why there is no field for it here.
+ *
+ * VFS absorbs the driver's staging limit (CDEV_MAX_IO), so a successful reply
+ * is the whole count; a short return is still handled by the caller.
+ */
+static long minixrs_write(int fd, const void *buf, size_t len)
+{
+	message m = { 0 };
+	long rc;
+
+	/* The length field is an i32 and VFS rejects a negative one. Clamping
+	 * rather than failing keeps this a short write, which every caller in
+	 * musl already loops on. */
+	if (len > (size_t)INT32_MAX)
+		len = (size_t)INT32_MAX;
+
+	m.m_type = VFS_WRITE;
+	put_i32(&m, VFS_FD_OFF, fd);
+	put_i32(&m, VFS_LEN_OFF, (int32_t)len);
+	put_u64(&m, VFS_BUF_OFF, (uint64_t)(uintptr_t)buf);
+
+	/* A non-OK trap result is the kernel refusing the IPC itself (a dead
+	 * endpoint, a privilege denial); VFS's own answer rides in m_type. Both
+	 * are already negative errnos, so either goes straight back to
+	 * syscall_ret. */
+	rc = __minixrs_ipc(VFS_EP, SENDREC, &m);
+	if (rc != OK)
+		return rc;
+	return m.m_type;
+}
+
+/*
+ * writev(2): one VFS_WRITE per iovec, counts summed.
+ *
+ * This is the syscall that actually carries stdio -- __stdio_write always calls
+ * writev, never write, and it loops until the returned count equals the total
+ * it asked for. So the count returned here must be exact: over-reporting makes
+ * musl believe a flush completed that did not.
+ *
+ * On an error after partial progress the progress is reported rather than the
+ * error, which is what POSIX requires: those bytes really did reach the
+ * console, and the caller re-drives the remainder and sees the error then.
+ */
+static long minixrs_writev(int fd, const struct iovec *iov, int count)
+{
+	long total = 0;
+	int i;
+
+	/* POSIX: an iovcnt outside [0, IOV_MAX] is EINVAL, and a NULL vector
+	 * with anything to write is not something to discover by dereferencing
+	 * it. A zero count is legal and writes nothing. */
+	if (count < 0 || count > IOV_MAX)
+		return -EINVAL;
+	if (count > 0 && !iov)
+		return -EFAULT;
+
+	for (i = 0; i < count; i++) {
+		size_t len = iov[i].iov_len;
+		long n;
+
+		if (len == 0)
+			continue;
+
+		n = minixrs_write(fd, iov[i].iov_base, len);
+		if (n < 0)
+			return total ? total : n;
+
+		total += n;
+
+		/* A short write ends the call -- writev is allowed to be
+		 * short, and continuing would silently reorder the stream. */
+		if ((size_t)n < len)
+			break;
+	}
+
+	return total;
+}
+
+/*
+ * exit(2) / exit_group(2), as PM_EXIT. Does not return: PM tears the process
+ * down via SYS_EXIT rather than replying, so the SENDREC never completes.
+ */
+_Noreturn static void minixrs_exit(int status)
+{
+	message m = { 0 };
+
+	m.m_type = PM_EXIT;
+	put_i32(&m, PM_EXIT_STATUS_OFF, status);
+	(void)__minixrs_ipc(PM_EP, SENDREC, &m);
+
+	/* Unreachable. The empty-asm barrier keeps this a real loop rather than
+	 * something the compiler may assume cannot be entered. */
+	for (;;)
+		__asm__ __volatile__ ("" ::: "memory");
+}
+
+long __minixrs_syscall(long n, long a, long b, long c, long d, long e, long f)
+{
+	(void)d;
+	(void)e;
+	(void)f;
+
+	switch (n) {
+	case SYS_writev:
+		return minixrs_writev((int)a, (const struct iovec *)b, (int)c);
+
+	case SYS_write:
+		return minixrs_write((int)a, (const void *)b, (size_t)c);
+
+	case SYS_exit_group:
+	case SYS_exit:
+		minixrs_exit((int)a);
+		/* minixrs_exit is _Noreturn, so this does not fall through to the
+		 * next case. Spelled out because a reader scanning for `break`
+		 * cannot see that from here. */
+		__builtin_unreachable();
+
+	/* No threads yet, so there is exactly one and its id is stable. musl
+	 * only stores this in the thread struct. */
+	case SYS_set_tid_address:
+		return 1;
+
+	/* Not a terminal as far as libc is concerned. See the header comment:
+	 * this is what makes stdout fully buffered. */
+	case SYS_ioctl:
+		return -ENOTTY;
+
+	/*
+	 * Everything else: close, lseek, ppoll, openat, mmap, futex, read...
+	 * These are referenced by code that links but does not run in the
+	 * slice-5.6 milestone. Real open/read/close arrive with the filesystem
+	 * (slice 5.8+); until then -ENOSYS is the honest answer, and it is one
+	 * musl's callers already handle.
+	 */
+	default:
+		return -ENOSYS;
+	}
+}
diff --git a/src/minixrs/ipc.c b/src/minixrs/ipc.c
new file mode 100644
index 00000000..abeae64c
--- /dev/null
+++ b/src/minixrs/ipc.c
@@ -0,0 +1,40 @@
+/*
+ * The minix.rs IPC trap.
+ *
+ * This is the one place in libc that executes `svc`. It reproduces the aarch64
+ * trap ABI fixed by the kernel's trap.S exactly, and is the C twin of the Rust
+ * `minixrs-ipc` crate (minixrs-ipc/src/lib.rs) -- the two must stay byte-identical
+ * in behaviour, because servers written in Rust and clients written in C meet
+ * on this interface:
+ *
+ *   x0 = endpoint (src/dest/src_dest); also receives the result
+ *   x1 = IPC primitive number (SEND / RECEIVE / SENDREC / NOTIFY / SENDNB)
+ *   x2 = pointer to the 104-byte message buffer
+ *   svc #0; the kernel's do_ipc writes the result into the saved x0
+ *
+ * The kernel saves and restores x0..x30 around the trap, so from the caller's
+ * view only x0 is clobbered. It is emphatically NOT a memory-clean asm: the
+ * kernel reads the outgoing message out of *m and writes the reply back into
+ * it, so the "memory" clobber is load-bearing -- without it the compiler may
+ * cache payload bytes across the call and read a stale reply.
+ */
+
+#include <minixrs/ipc.h>
+
+#include "minixrs_internal.h"
+
+long __minixrs_ipc(long endpoint, long primitive, message *m)
+{
+	register long x0 __asm__("x0") = endpoint;
+	register long x1 __asm__("x1") = primitive;
+	register long x2 __asm__("x2") = (long)(__UINTPTR_TYPE__)m;
+
+	__asm__ __volatile__ (
+		"svc #0"
+		: "+r"(x0)
+		: "r"(x1), "r"(x2)
+		: "memory", "cc"
+	);
+
+	return x0;
+}
diff --git a/src/minixrs/minixrs_internal.h b/src/minixrs/minixrs_internal.h
new file mode 100644
index 00000000..8339f19a
--- /dev/null
+++ b/src/minixrs/minixrs_internal.h
@@ -0,0 +1,43 @@
+/*
+ * Internal declarations shared by the minix.rs port files in this directory.
+ *
+ * Deliberately not installed into the sysroot: nothing outside libc calls the
+ * IPC trap directly. A C program reaches the servers through ordinary POSIX
+ * wrappers, which is the entire point of the port.
+ */
+
+#ifndef _MINIXRS_INTERNAL_H
+#define _MINIXRS_INTERNAL_H
+
+#include <features.h>
+
+#include <minixrs/ipc.h>
+
+/* The IPC trap (ipc.c). Returns the kernel's reply code in x0. */
+hidden long __minixrs_ipc(long endpoint, long primitive, message *m);
+
+/*
+ * The syscall dispatcher (_syscall.c). Declared here as well as in
+ * arch/aarch64/syscall_arch.h so the DEFINITION sees the `hidden` attribute:
+ * _syscall.c includes the public <sys/syscall.h>, not src/internal/syscall.h,
+ * so without this it would be compiled with default visibility while every
+ * caller expects hidden. Harmless in a static-only build, wrong the moment
+ * --disable-shared is dropped, and inconsistent with __minixrs_ipc either way.
+ *
+ * src/thread/aarch64/syscall_cp.s branches here too, which is why it is not
+ * static.
+ */
+hidden long __minixrs_syscall(long n, long a, long b, long c, long d, long e,
+                              long f);
+
+/*
+ * Offset of the exit status in a PM_EXIT payload (i32).
+ *
+ * Not emitted by tools/gen-c-headers: on the Rust side the field is documented
+ * in callnr.rs's PM_EXIT doc comment rather than named as a constant, and
+ * userland/worker writes it as payload[0..4] the same way. Spelled out here so
+ * the C side states the offset it depends on instead of burying a bare 0.
+ */
+#define PM_EXIT_STATUS_OFF 0
+
+#endif
diff --git a/src/thread/aarch64/syscall_cp.s b/src/thread/aarch64/syscall_cp.s
index 41db68af..aa64582b 100644
--- a/src/thread/aarch64/syscall_cp.s
+++ b/src/thread/aarch64/syscall_cp.s
@@ -1,8 +1,35 @@
 // __syscall_cp_asm(&self->cancel, nr, u, v, w, x, y, z)
 //                  x0             x1  x2 x3 x4 x5 x6 x7
-
-// syscall(nr, u, v, w, x, y, z)
-//         x8  x0 x1 x2 x3 x4 x5
+//
+// __minixrs_syscall(nr, a, b, c, d, e, f)
+//                   x0  x1 x2 x3 x4 x5 x6
+//
+// minix.rs port: upstream ended this sequence with a bare `svc 0` carrying the
+// Linux syscall number in x8. On minix.rs that instruction means something
+// completely different -- the kernel's only `svc` ABI is the IPC trap, which
+// reads x0 as an endpoint, x1 as a primitive and x2 as a message pointer -- so
+// executing it would aim a garbage IPC call at a garbage endpoint.
+//
+// It is NOT dead code, and it is not reachable today only by accident of
+// linkage: src/thread/__syscall_cp.c supplies a WEAK __syscall_cp_c that goes
+// through __syscall (and therefore through the port), while pthread_cancel.c
+// supplies a STRONG one that calls this function. Whichever translation unit
+// gets pulled in decides. A program that references pthread_cancel -- any real
+// use of threads -- silently switches every cancellable syscall (read, write,
+// open, close, ...) onto the raw path. So this forwards to the port instead.
+//
+// Cancellation is a no-op here: minix.rs has no threads yet, so self->cancel is
+// never set. The __cp_begin / __cp_end / __cp_cancel labels are kept exactly as
+// upstream defines them, because pthread_cancel.c's signal handler compares a
+// faulting PC against that window and __cancel is what it branches to. Keeping
+// the shape means the day threads arrive, only this file changes.
+//
+// Note what the tail branch costs: control leaves before __cp_end, so the
+// __cp_begin..__cp_end window no longer brackets the trap and an async cancel
+// landing mid-call would not be recognised. That is precisely the no-op above,
+// stated as a limitation rather than left to be discovered -- restoring it
+// means `bl` plus saving x30, which changes the frame the handler inspects, and
+// is work for the slice that actually adds threads.
 
 .global __cp_begin
 .hidden __cp_begin
@@ -11,6 +38,7 @@
 .global __cp_cancel
 .hidden __cp_cancel
 .hidden __cancel
+.hidden __minixrs_syscall
 .global __syscall_cp_asm
 .hidden __syscall_cp_asm
 .type __syscall_cp_asm,%function
@@ -18,14 +46,16 @@ __syscall_cp_asm:
 __cp_begin:
 	ldr w0,[x0]
 	cbnz w0,__cp_cancel
-	mov x8,x1
-	mov x0,x2
-	mov x1,x3
-	mov x2,x4
-	mov x3,x5
-	mov x4,x6
-	mov x5,x7
-	svc 0
+	// Drop the cancel-flag pointer and shift (nr, u..z) down one register, so
+	// x0 = nr and x1..x6 = the six arguments -- __minixrs_syscall's C ABI.
+	mov x0,x1
+	mov x1,x2
+	mov x2,x3
+	mov x3,x4
+	mov x4,x5
+	mov x5,x6
+	mov x6,x7
+	b __minixrs_syscall
 __cp_end:
 	ret
 __cp_cancel:
-- 
2.50.1 (Apple Git-155)

